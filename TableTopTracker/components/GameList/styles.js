import { StyleSheet } from 'react-native';

// placeholder styles (to be replaced w/ themes, probably)
const styles = StyleSheet.create({
  list: {
    flex: 1,
    padding: 15
  },
  card: {
    marginBottom: 10
  }
});

export default styles;